<?xml version="1.0" encoding="utf-8"?>
<!--
  Development-only network security config.
  Cleartext (HTTP) is permitted solely for local backend hosts used by the
  emulator/simulator/desktop during development. Staging and production do not
  use this config and therefore reject cleartext by default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
